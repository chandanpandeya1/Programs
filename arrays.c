#include <stdio.h>
int main()
{
    int marks[5] = {2, 3, 4, 2, 4};
   for(int i = 0; i < 4; i++)
    {
        printf(" the value of %d the elment of the array is %d\n", i, marks[i]);
    }
    return 0;
}
